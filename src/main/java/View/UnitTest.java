package View;

import javafx.application.Application;
import javafx.fxml.FXMLLoader;
import javafx.scene.Group;
import javafx.scene.Scene;
import javafx.scene.image.Image;
import javafx.scene.paint.Color;
import javafx.stage.Stage;

import java.io.FileInputStream;
import java.io.IOException;

//appel des formes créées

/*public class UnitTest extends Application {

    @Override
    public void start(Stage stage) throws IOException {
        FXMLLoader fxmlLoader = new FXMLLoader(UnitTest.class.getResource("homePage.fxml"));
        //Stage stage = new Stage();
        Group root = new Group();
        Scene scene = new Scene(root, Color.BLACK);
        //Scene scene = new Scene(fxmlLoader.load());
        Image icon = new Image(new FileInputStream("src\\main\\resources\\Image\\ECE_LOGO.png"));
        stage.getIcons().add(icon);
        //stage.setScene(scene);
        stage.setTitle("homePage");
        stage.setFullScreen(true);

        stage.setScene(scene);

        stage.show();
    }

    public static void main(String[] args) {
        launch();
    }

}*/